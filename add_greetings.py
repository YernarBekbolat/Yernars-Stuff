def add_greetings(names):
  start = []
  for name in names:
    start.append("Hello, " + name)
  return start

print(add_greetings(["Owen", "Max", "Sophie"]))

#Добавляет к листу стрингов "Hello, " (можно заменить чем угодно)